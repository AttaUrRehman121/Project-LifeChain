{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "cd LifeChain && pip install -r requirements.txt"
    },
    "start": {
        "cmd": "cd LifeChain && gunicorn LifeChain.wsgi:application --bind 0.0.0.0:$PORT"
    }
}