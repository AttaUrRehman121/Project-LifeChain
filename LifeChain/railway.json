{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "pip install -r requirements.txt"
    },
    "start": {
        "cmd": "gunicorn LifeChain.wsgi:application --bind 0.0.0.0:$PORT"
    }
}